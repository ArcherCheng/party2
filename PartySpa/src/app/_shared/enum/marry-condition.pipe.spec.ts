import { MarryConditionPipe } from './marry-condition.pipe';

describe('MarryConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new MarryConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
