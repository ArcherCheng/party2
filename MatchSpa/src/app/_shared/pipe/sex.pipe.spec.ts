/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SexPipe } from './sex.pipe';

describe('Pipe: Sexe', () => {
  it('create an instance', () => {
    let pipe = new SexPipe();
    expect(pipe).toBeTruthy();
  });
});
