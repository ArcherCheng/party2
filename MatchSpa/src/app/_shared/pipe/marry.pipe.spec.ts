/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MarryPipe } from './marry.pipe';

describe('Pipe: Marrye', () => {
  it('create an instance', () => {
    let pipe = new MarryPipe();
    expect(pipe).toBeTruthy();
  });
});
